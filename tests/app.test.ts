describe('Basic App Test', () => {
  it('should pass a simple truthiness test', () => {
    expect(true).toBe(true);
  });

  it('should correctly perform a basic arithmetic operation', () => {
    expect(1 + 1).toBe(2);
  });
});
