// Jest setup file (e.g., for global mocks or initializations)

import 'jest-canvas-mock';

// You can add other global test setup here if needed in the future.
// For example, mocking localStorage, sessionStorage, etc.

console.log('jest-canvas-mock initialized via jest.setup.ts');
